# Avatar picker

Added to the sidebar profile:
- six built-in avatar choices
- upload JPG/PNG/WebP photo up to 1 MB
- remove avatar
- click avatar to open/close picker
- selected avatar is saved through the existing `setProfile` path

No new database tables or Supabase schema changes were introduced.
